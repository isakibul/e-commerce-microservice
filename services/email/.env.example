PORT=4005
SERVICE_NAME=Email-Service

DATABASE_URL=postgresql://postgres:postgres@localhost:5433/email?schema=public

INTERNAL_GATEWAY_SECRET=replace-with-a-long-random-secret

# MailHog defaults from docker-compose.yaml.
SMTP_HOST=localhost
SMTP_PORT=1025
DEFAULT_EMAIL_SENDER=admin@example.com

QUEUE_URL=amqp://guest:guest@localhost:5672
EMAIL_EXCHANGE=email.events
ORDER_EXCHANGE=order.events
ORDER_RETRY_EXCHANGE=order.events.retry
ORDER_DLX=order.events.dlx
QUEUE_RETRY_DELAY_MS=5000
QUEUE_MAX_RETRIES=3
